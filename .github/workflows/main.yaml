name: Main CI

on:
  push:
    branches:
      - main

jobs:

  global-install: 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache node_modules and Vite cache
        uses: actions/cache@v4
        with:
          path: |
            ./packages/react/node_modules
            ./packages/react/.vite
            ./packages/angular/node_modules
            ./packages/angular/.vite
          key: deps-node20-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            deps-node20-

      - name: Install dependencies
        run: npm ci --prefer-offline

  quality-react:
    runs-on: ubuntu-latest
    needs: global-install
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Restore cache node_modules and Vite cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ./packages/react/node_modules
            ./packages/react/.vite
          key: deps-node20-${{ hashFiles('package-lock.json') }}

      - name: Run tests
        working-directory: packages/react
        run: echo "No tests for React package"

  build-react:
    runs-on: ubuntu-latest
    needs: quality-react
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Restore cache node_modules and Vite cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ./packages/react/node_modules
            ./packages/react/.vite
          key: deps-node20-${{ hashFiles('package-lock.json') }}
      
      - name: Cache react build output
        uses: actions/cache@v4
        with:
          path: |
            ./packages/react/dist
          key: react-build-${{ hashFiles('./packages/react/dist/react-package.js') }}

      - name: Build project
        working-directory: packages/react-package
        run: npm run build

  quality-angular:
    runs-on: ubuntu-latest
    needs: global-install
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: Restore cache node_modules and Vite cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ./packages/angular/node_modules
            ./packages/angular/.vite
          key: deps-node20-${{ hashFiles('package-lock.json') }}

      - name: Run tests
        working-directory: packages/angular
        run: echo "No tests for Angular package"
  
  build-angular:
    runs-on: ubuntu-latest
    needs: quality-angular
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
  
      - name: Restore cache node_modules and Vite cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ./packages/angular/node_modules
            ./packages/angular/.vite
          key: deps-node20-${{ hashFiles('package-lock.json') }}
    
      - name: Build project
        working-directory: packages/angular-package
        run: echo "No build for Angular package"

  
  prepare-release:
    needs: [build-react, build-angular]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci --prefer-offline

      - name: Create/Update Release Pull Request
        uses: changesets/action@v1.4.9
        with:
          title: "chore(release): update package versions"
          commit: "chore(release): update package versions"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
