import { DynamicTabItemProps } from '../../../../../core';
declare const DynamicTabItem: ({ id, index, title, parentId, appearance, isActive, onClick, onClickClose, onChangeTabTitle, compactSpacing, editable, badgeContent, badgeCount, badgeIcon, badgeSize, badgeType, iconName, closable, updateIndicator, isHidden, setIsMoving: setIsMovingExternal, isMoving: isMovingExternal, }: DynamicTabItemProps) => import("react").JSX.Element;
export default DynamicTabItem;
