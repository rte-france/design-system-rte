- La taille conditionne l'affichage du badge en fonction de son content
  - Pour un badge avec `content="empty"`, toutes les tailles s'affichent.
  - Pour un badge avec `content="number"`, seules les tailles `m` et `l` s'affichent.
  - Pour un badge avec `content="icon"`, seules les tailles `m` et `l` s'affichent.

- En `content="number"` la valeur maximale affichée est 99, un `+` est ajouté pour les nombres suivants.
