### Text length

The tooltip label is limited to **two lines of text**.

Content that exceeds this height is clipped (`overflow: hidden` with a fixed max height). Prefer short, concise labels. Longer explanations belong in another UI pattern (for example a popover or modal).
