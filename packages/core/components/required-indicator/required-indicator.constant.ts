import { RequirementIndicator } from "./required-indicator.interface";

export const REQUIREMENT_INDICATOR_VALUE: Record<RequirementIndicator, string> = {
  required: "(obligatoire)",
  requiredIcon: "*",
  optional: "(facultatif)",
};
