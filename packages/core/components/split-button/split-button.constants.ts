import { SplitButtonSize } from "./split-button.interface";

export const splitButtonLeftIconSize: Record<SplitButtonSize, number> = {
  s: 16,
  m: 20,
  l: 24,
};

export const splitButtonRightIconSize: Record<SplitButtonSize, number> = {
  s: 20,
  m: 24,
  l: 28,
};
